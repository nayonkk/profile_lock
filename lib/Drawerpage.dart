import 'package:flutter/material.dart';


class Drawerpages extends StatelessWidget {
  const Drawerpages({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Drawer(backgroundColor: Colors.amber,);
  }
}
